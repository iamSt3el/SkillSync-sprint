apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: skillsync
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review-service
  template:
    metadata:
      labels:
        app: review-service
    spec:
      containers:
        - name: review-service
          image: asia-south1-docker.pkg.dev/project-00909acc-d419-4f02-8a1/skillsync-repo/review-service:latest
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: skillsync-config
          env:
            - name: SPRING_APPLICATION_NAME
              value: "review-service"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:3306/reviews-service"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: skillsync-secrets
                  key: MYSQL_ROOT_PASSWORD
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8087
            initialDelaySeconds: 45
            periodSeconds: 10
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8087
            initialDelaySeconds: 150
            periodSeconds: 15

---
apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: skillsync
spec:
  selector:
    app: review-service
  ports:
    - port: 8087
      targetPort: 8087
  type: ClusterIP

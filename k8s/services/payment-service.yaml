apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: skillsync
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: AWS_ACCOUNT_ID_PLACEHOLDER.dkr.ecr.REGION_PLACEHOLDER.amazonaws.com/skillsync-repo/payment-service:latest
          ports:
            - containerPort: 8089
          envFrom:
            - configMapRef:
                name: skillsync-config
          env:
            - name: SPRING_APPLICATION_NAME
              value: "payment-service"
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms64m -Xmx192m"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:3306/payment_service"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: skillsync-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: RAZORPAY_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: skillsync-secrets
                  key: RAZORPAY_KEY_ID
            - name: RAZORPAY_KEY_SECRET
              valueFrom:
                secretKeyRef:
                  name: skillsync-secrets
                  key: RAZORPAY_KEY_SECRET
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx384m"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8089
            initialDelaySeconds: 120
            periodSeconds: 10
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8089
            initialDelaySeconds: 300
            periodSeconds: 15

---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: skillsync
spec:
  selector:
    app: payment-service
  ports:
    - port: 8089
      targetPort: 8089
  type: ClusterIP
